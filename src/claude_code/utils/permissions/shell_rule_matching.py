"""Shell permission rule matching (``utils/permissions/shellRuleMatching.ts``) — stub."""

from __future__ import annotations

from typing import Any


def match_shell_rule(_command: str, _rules: list[Any]) -> Any | None:
    return None

"""Lazy-loaded session UI (Ink / TUI parity). Wire remote session presentation here."""

from __future__ import annotations

# Imported via CommandSpec.load_symbol when the interactive host resolves JSX commands.

__all__: list[str] = []

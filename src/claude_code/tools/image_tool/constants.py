"""Image processor bridge — TS source: tools/FileReadTool/imageProcessor.ts (sharp / napi)."""

IMAGE_TOOL_NAME = "ImageProcessor"

DESCRIPTION = "Load optional native image processing (resize, format conversion)."

"""No ListDirTool in this leak — directory listing companion to Read/Glob."""

LIST_DIR_TOOL_NAME = "ListDir"

DESCRIPTION = (
    "List files and subdirectories in a directory with metadata (name, type). "
    "Use instead of Read when the path is a folder."
)
